import React from 'react';
import Layout from "@layout/index";
import MatchesContainer from "@components/matches/Matches";

const Matches = () => {
  return (
    <Layout>
      <MatchesContainer/>
    </Layout>
  );
};

export default Matches;