import TableColumnFactory from "./tableColumnFactory.ts";

/**
 * These columns are used in the home page.
 */
export default function getHomeTableColumns() {
  const tableColumnFactory = new TableColumnFactory();

  return [];
}
